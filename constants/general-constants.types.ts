export type GeneralConstantsType = {
  // TODO: use location value from env to store files on consumer's api, move uploadLocation to env constant
  uploadLocation: string;
  development: string;
  staging: string;
  production: string;
};
